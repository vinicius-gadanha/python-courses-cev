def metade(x):
    res = x / 2
    return res


def dobro(x):
    res = x * 2
    return res


def aumentar(x, porc):
    res = x + (x * porc / 100)
    return res


def diminuir(x, porc):
    res = x - (x * porc / 100)
    return res
